const initialState = {
    name: "realmName",
    img: "IMG",
    candles: ["candle1", "candle2"]
};

const reducer = (state = initialState, action) => {
    // switch (action.type) {
    //     case actionTypes.SET_REALM:
    //         return {
    //             ...state,
    //         };
    //     default:
            return state;
    // }
};

export default reducer;